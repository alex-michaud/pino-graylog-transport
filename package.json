{
  "name": "pino-graylog-transport",
  "version": "1.0.0",
  "description": "Pino transport that send log to graylog",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha test/**/*.test.js",
    "test:load": "k6 run test/load/load-test.js",
    "lint": "eslint lib test",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "keywords": [
    "pino",
    "transport",
    "graylog",
    "logging",
    "gelf"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pino-abstract-transport": "^1.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "mocha": "^10.0.0",
    "chai": "^4.3.0",
    "pino": "^8.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
